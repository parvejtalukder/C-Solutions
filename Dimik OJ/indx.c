#include <stdio.h>
int main() {
    const int size 5;
    int num[size]size]; 
    int i, j, k, h;
    num[0][0] = 12;
    for (i = 0; i < size; i++) {
        for (j = 0; j <= i; j++) {
            printf("%d", num[i][j]);
        }
    }
    return 0;
}